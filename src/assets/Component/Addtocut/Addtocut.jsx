import React from 'react';

const Addtocut = (addtocat) => {
    const { recipe_name,calories,preparing_time } =addtocat;
    return (
        <div>
            
        </div>
    );
};

export default Addtocut;